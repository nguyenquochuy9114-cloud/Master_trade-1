services:
  - type: web
    name: MasterTradeBot
    env: python
    buildCommand: pip install -r requirements.txt --no-cache-dir
    startCommand: python -u bot.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.9
